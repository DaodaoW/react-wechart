import styled from 'styled-components';
import bg from '@/assets/bindAccount/bg.png';

export const Main = styled.div<{}>`
  width: 100%;
  height: 14rem;
  background-image: url(${bg});
  background-size: cover;
  background-repeat: no-repeat;
  padding: 1.5rem 1rem;
  margin-bottom: 7rem;
`